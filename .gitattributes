# Робоча копія лежить на Windows-диску, збірка ончейн-програми йде в WSL.
# Без цього WSL отримує CRLF і shell-скрипти перестають бути виконуваними
# з причин, які виглядають як завгодно, тільки не як кінці рядків.
* text=auto eol=lf

*.so binary
